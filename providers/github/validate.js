module.exports = (token, repo) => {
  // 1. Make sure the user has write access to this repo.
  // 2. Check for a `.uncms` configuration in the repo or `package.json`.
  // 3. Determine the return URL and redirect the user with their token.
};
